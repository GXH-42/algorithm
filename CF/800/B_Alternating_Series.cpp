#include <bits/stdc++.h>
#define endl '\n'
using namespace std;
#define int long long

signed main(){
    ios_base::sync_with_stdio(0);
	cin.tie(0);
    //输出最优好数组。好数组的约束条件如下：
    //1.相邻元素乘积为负
    //2.相邻两个及以上的子数组元素和为正
    
    //最优条件如下：
    //数组元素绝对值在词典上最小
   
    int t;
    cin >> t;
    while(t--){
        int n;
        cin>>n;
        for(int i = 0; i < n; i++){
            

        }
    }
}